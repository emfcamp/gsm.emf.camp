{
  "name": "gsm.emf.camp",
  "version": "1.0.0",
  "description": "These are the source files for <https://gsm.emf.camp>.",
  "scripts": {
    "build": "npx hugo --minify --gc",
    "dev": "npx hugo server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "autoprefixer": "^10.5.0",
    "hugo-extended": "^0.162.1",
    "postcss": "^8.5.15",
    "postcss-cli": "^11.0.1",
    "sass": "^1.100.0"
  }
}
